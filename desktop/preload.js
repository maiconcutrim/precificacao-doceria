// Preload do Electron.
// Por enquanto vazio: a interface conversa com o servidor local por HTTP (fetch),
// não precisando de ponte com o Node do processo principal.
// Reservado para futuras integrações nativas (ex.: caminho de backup, impressão).
